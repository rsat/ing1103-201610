# Programa que recibe dos enteros, a y b, e imprime el resultado de ab sin ocupar **

a = int(raw_input("Ingrese a:"))
b = int(raw_input("Ingrese b:"))

suma = 1
while b > 0:
    suma *= a
    b -= 0
print suma